{% extends 'base.html.twig' %}

{% block head %}
    <style>
        .boton-email {
            background-color: #F16E15;
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            transition-duration: 0.4s;
        }

        .boton-email:hover {
            background-color: #E15E10;
            color: white;
        }
    </style>
{% endblock %}

{% block body %}
    <tr>
        <td style="text-align:center;">
            <h1>Estimad@ {{ name|capitalize }}.</h1>
            <p>Hemos recibido una solicitud para restablecer la contraseña de tu cuenta en Drinksify.</p>
            <p>Para continuar con el proceso de restablecimiento, por favor haz clic en el siguiente botón:</p>

            <a href="{{ url }}" class="boton-email">Cambiar contraseña</a>
            <p>En caso de que el botón no funcione puedes hacer click en este enlace <a href="{{ url }}">{{ url }}</a></p>

            <p>Saludos cordiales,</p>
            <p>Gracias por usar Drinksify.</p>
        </td>
    </tr>
{% endblock %}